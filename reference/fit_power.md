# Power Law

Plots power law fit

## Usage

``` r
fit_power(graph)
```

## Arguments

- graph:

  iGraph object

## Examples

``` r
if (FALSE) { # \dontrun{
netDir <- make.netDir(OD_Sample)
fit_power(netDir$gDir)
} # }
```
